# 面向人工智能学习

问题
```
```
回答
```
1.首先PHP是弱类型语言，变量不需要先定义再使用。php控制流有 if-else,while,for,foreach,match(PHP8.0版本)。2.对常用的组件进行封装以复用。一个方法只完成一件事，进行方法的封装，同一类方法封装在同一个类下。使用compose的autoload和命名空间实现对类的索引。3.在开发中，使用过ThinkPHP、Hyperf、easyswoole框架，Laravel和ThinkPHP框架类似。Hyperf框架和ThinkPHP框架，吸收了很多Laravel的优点。4.在开发中，使用debug的方式，对程序进行调试。在开发过程中，和一个模块开发完成之后，都会进行测试。问题排查，即先定位问题，然后复现错误，并进行代码调试和修复，修复完成之后对代码进行测试。5.熟练掌握mysql的开发，优化方面，先根据需求，设计合适的字段，对于第三方请求的数据，不清楚字段的大小，会先设置稍微大一点的字段。然后以自增id的方式添加主键索引。并在后续的开发和使用中，对部分字段，考虑是否增加索引。优化策略是，找到慢sql的语句，进行语句分析，并看是否走了索引，如果没有走索引，对sql语句进行调整，或者建立适当的索引。对于数据库字段数据比较大时，则考虑分库分表的策略。6.包括sql注入，使用PDO，对参数和sql语句分离，不解析语句的内容；xss攻击，即对数据数据框，使用js代码，使用户体验降低；上传可执行脚本。7.使用过PHPunit工具。8.PHP是单线程，对并发支持并不是很好。我目前的项目是单服务器的，qps并不是很高。我想，可以通过服务划分，分开部署，并使用ngnix转发，降低服务器的压力，然后以排队的方式，限制请求，然后以队列的方式，异步投递消息和非同步消息。使用redis或者多级缓存对数据进行缓存，减少mysql的压力。迁移项目到PHP高一点的版本。比如8.1版本支持opencache。9.读过一点PHP的源码，PHP底层使用C99实现的。PHP的弱类型，其实是底层用联合体的形式对数据类型进行了判断，而非真正不需要定于类型。gc是三色标记法，如果是被引用的变量，则会有引用计数。目前在使用下班时间学习C语言，希望能够为PHP添砖加瓦。10.公司有自己的前端开发人员，我工作中接触的前端并不多。如果要我使用前端框架模板之类，我会选用TS和vue进行开发。谢谢。我回答完毕。请你判断我的PHP开发能力，或者你还需要继续提问，如果不想提问请对我的面试表现进行评价
```